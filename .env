# Django Settings
SECRET_KEY=django-insecure-test-key-for-local-development-only
DEBUG=True
ALLOWED_HOSTS=*
ENVIRONMENT=development

# Database
DATABASE_URL=sqlite:///db.sqlite3

# Redis & Celery (disabled for local testing without Redis)
REDIS_URL=
CELERY_BROKER_URL=
CELERY_RESULT_BACKEND=

# Static & Media
STATIC_ROOT=staticfiles
MEDIA_ROOT=media

# Email
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=noreply@localhost
ADMIN_EMAIL=admin@localhost

# Site URLs
SITE_URL=http://localhost:8000
NEXTJS_URL=http://localhost:3000

# Secrets
REVALIDATE_SECRET=test-revalidate-secret-for-local-dev
ANALYTICS_SECRET=test-analytics-secret-for-local-dev

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Throttle Rates
ANON_THROTTLE_RATE=1000/hour
USER_THROTTLE_RATE=10000/hour

# Logging
DJANGO_LOG_LEVEL=DEBUG

# API Contact
CONTACT_EMAIL=api@localhost

# CDN Configuration (optional - disabled for local)
CDN_PROVIDER=
